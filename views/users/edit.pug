extends ../layout

block content
  h1= title

  p Editer l'utilisateur :

  form(action=action method='post')
    table(border=1)
      tr
        th Id
        td= user._id
      tr
        th Pseudo
        td
          input(name='username' value=user.username placeholder=user.username)
      tr
        th Mot de passe
        td
          input(name='password' value=user.password)
      tr
        th Prénom
        td
          input(name='name' value=user.name)
      tr
        th Email
        td
          input(name='mail' value=user.mail)

    input(type='submit', value='Envoyer')
